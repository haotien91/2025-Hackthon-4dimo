# Art Pass - 文化展演平台技術架構文件

> **MVP 版本** - 專注於核心功能，快速迭代

## 目錄

1. [專案概述](#專案概述)
2. [技術棧](#技術棧)
3. [整體架構](#整體架構)
4. [頁面設計](#頁面設計)
5. [組件設計](#組件設計)
6. [API 設計](#api-設計)
7. [數據流](#數據流)
8. [目錄結構](#目錄結構)
9. [開發階段規劃](#開發階段規劃)

---

## 專案概述

### 目標
打造一個現代化的文化展演探索平台，讓用戶能夠：
- 🔥 發現熱門和近期展演
- 🔍 根據條件篩選展演
- 📍 探索附近的展演場館
- 📖 記錄觀展歷程

### 使用場景
- **Flutter App (TownPass)** 透過 WebView 渲染 Next.js 應用
- **Web 獨立訪問** (可選)

---

## 技術棧

### 前端框架
```json
{
  "核心": "Next.js 16 (App Router)",
  "語言": "TypeScript",
  "樣式": "Tailwind CSS 4",
  "React版本": "19.2.0"
}
```

### 關鍵技術選擇

| 功能 | 技術方案 | 原因 |
|------|---------|------|
| 路由 | Next.js App Router | 檔案系統路由，簡單直觀 |
| 樣式 | Tailwind CSS | 快速開發，響應式設計 |
| 地圖 | Google Maps API | 可靠且功能完整 |
| 數據存儲 | LocalStorage (MVP) → SQLite/Supabase (未來) | MVP 階段先用瀏覽器存儲，後續擴展 |
| 狀態管理 | React Context / useState | MVP 不需要 Redux，保持簡單 |
| 圖片優化 | Next.js Image | 內建優化，懶加載 |

---

## 整體架構

```
┌─────────────────────────────────────────────────┐
│           Flutter App (TownPass)                │
│                  WebView                        │
└─────────────────┬───────────────────────────────┘
                  │
                  │ 渲染
                  ▼
┌─────────────────────────────────────────────────┐
│            Next.js App (art-pass)               │
│                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  首頁    │  │找展演    │  │找身邊    │     │
│  │  /       │  │/explore  │  │/nearby   │     │
│  └──────────┘  └──────────┘  └──────────┘     │
│                                                 │
│              ┌──────────┐                      │
│              │找記憶    │                      │
│              │/memory │                      │
│              └──────────┘                      │
│                                                 │
│           ┌─────────────────┐                  │
│           │  API Routes     │                  │
│           │  /api/*         │                  │
│           └─────────────────┘                  │
└─────────────────┬───────────────────────────────┘
                  │
                  │ 數據來源
                  ▼
┌─────────────────────────────────────────────────┐
│         後端爬蟲 / 數據源                        │
│   JSON 格式展演資料 + 圖片                       │
└─────────────────────────────────────────────────┘
```

---

## 頁面設計

### 1. 首頁 `/`

#### 佈局
```
┌────────────────────────────────────────┐
│            Header                      │ ← 標題 (可選 logo)
├────────────────────────────────────────┤
│  🔥 熱門展演                           │
│  ┌──────┐  ┌──────┐  ┌──────┐        │
│  │      │  │      │  │      │  →     │ ← 橫向滾動
│  │ 圖片 │  │ 圖片 │  │ 圖片 │        │
│  │      │  │      │  │      │        │
│  │ 標題 │  │ 標題 │  │ 標題 │        │
│  └──────┘  └──────┘  └──────┘        │
├────────────────────────────────────────┤
│  📅 近期展演                           │
│  ┌──────┐  ┌──────┐  ┌──────┐        │
│  │      │  │      │  │      │  →     │
│  │ 圖片 │  │ 圖片 │  │ 圖片 │        │
│  │      │  │      │  │      │        │
│  │ 標題 │  │ 標題 │  │ 標題 │        │
│  └──────┘  └──────┘  └──────┘        │
├────────────────────────────────────────┤
│           Quick Actions                │
│  ┌──────┐  ┌──────┐  ┌──────┐        │
│  │ 🔍  │  │ 📍  │  │ 📖  │        │
│  │找展演│  │找身邊│  │找記憶│        │
│  └──────┘  └──────┘  └──────┘        │
└────────────────────────────────────────┘
```

#### 功能需求
- **熱門展演**: 根據某種指標（瀏覽數、評分等）排序，取前 10 筆
- **近期展演**: 根據開始時間排序，顯示即將開始的展演
- **卡片設計**: 
  - 固定寬度（約 280px）
  - 圖片比例 16:9
  - 顯示：圖片、標題、日期、地點
  - 右上角有愛心收藏按鈕
- **橫向滾動**: 使用 `overflow-x-scroll` + `scroll-snap`

#### 數據來源
- API: `/api/events/featured` (熱門)
- API: `/api/events/upcoming` (近期)

---

### 2. 找展演 `/explore`

#### 佈局
```
┌────────────────────────────────────────┐
│  ← 返回      找展演         [篩選]    │ ← Header + Filter Button
├────────────────────────────────────────┤
│  ┌────────────────────────────────┐   │
│  │         展演卡片 #1            │   │ ↕
│  │  ┌────┐                        │   │ 垂
│  │  │圖片│ 標題                   │   │ 直
│  │  │    │ 日期 | 地點    ♡      │   │ 滾
│  │  └────┘ 類型 | 票價           │   │ 動
│  └────────────────────────────────┘   │
│                                        │
│  ┌────────────────────────────────┐   │
│  │         展演卡片 #2            │   │
│  │  ┌────┐                        │   │
│  │  │圖片│ 標題                   │   │
│  │  │    │ 日期 | 地點    ♡      │   │
│  │  └────┘ 類型 | 票價           │   │
│  └────────────────────────────────┘   │
│                                        │
└────────────────────────────────────────┘
                                        
篩選彈窗（從底部滑出）:                   
┌────────────────────────────────────────┐
│              篩選條件                   │
│                                        │
│  地區: [台北市 ▼]                     │
│  分類: [全部 ▼]                       │
│  票價: [─────○─────] $0-1000         │
│  時間: [本週 ▼]                       │
│  場館: [全部 ▼]                       │
│                                        │
│  [重置]              [套用篩選]       │
└────────────────────────────────────────┘
```

#### 功能需求
- **卡片設計**: 類似 UberEats 的橫向卡片
  - 左側圖片（固定 120x120px）
  - 右側資訊（標題、日期、地點、類型、票價）
  - 右上角愛心收藏按鈕
- **篩選功能**:
  - 地區: 下拉選單（台北市、新北市等）
  - 分類: 下拉選單（音樂、展覽、戲劇等）
  - 票價: 滑桿（0-2000）
  - 時間: 下拉選單（今天、本週、本月、全部）
  - 場館: 下拉選單（故宮、中正紀念堂等）
- **彈窗設計**: 
  - 從底部滑出（bottom sheet）
  - 背景半透明遮罩
  - 點擊外部或「套用篩選」關閉

#### 數據來源
- API: `/api/events/list?filter={...}`

---

### 3. 找身邊 `/nearby`

#### 佈局
```
┌────────────────────────────────────────┐
│  ← 返回      找身邊                    │
├────────────────────────────────────────┤
│  ┌────────────────────────────────┐   │
│  │  目前場館: 國立故宮博物院      │   │ ← 場館資訊
│  └────────────────────────────────┘   │
│  ┌────────────────────────────────┐   │
│  │                                │   │
│  │         Google Map             │   │
│  │         (Markers)              │   │
│  │                                │   │
│  └────────────────────────────────┘   │
│                                        │
│  ┌───────────────────────────────────┐│
│  │展演卡片 #1  │ 展演卡片 #2  │ NEXT ││ ← 橫向滾動
│  │圖片         │ 圖片         │   →  ││
│  │標題         │ 標題         │      ││
│  │時間         │ 時間         │      ││
│  └───────────────────────────────────┘│
└────────────────────────────────────────┘
```

#### 功能需求
- **Google Maps 整合**:
  - 初始位置: 用戶當前位置（或預設台北市）
  - Markers: 每個展演場館一個 marker
  - 點擊 marker: 對應卡片滾動到該展演
- **底部卡片**:
  - 橫向滾動
  - 每個場館的展演按距離排序
  - 卡片固定寬度（約 300px）
  - 顯示：圖片、標題、時間、距離
- **智能 Focus**:
  - 當滾動到當前場館的最後一個展演
  - 繼續向右滑動會顯示 "NEXT" 提示
  - 放開後地圖自動 pan 到下一個場館
  - 卡片列表更新為下一個場館的展演
- **場館資訊顯示**:
  - 地圖上方黑底白字顯示當前場館名稱
  - 隨著滾動和 focus 變化更新

#### 數據來源
- API: `/api/events/nearby?lat={}&lng={}&radius={}`
- 使用瀏覽器 Geolocation API 獲取位置

---

### 4. 找記憶 `/memory`

#### 佈局
```
┌────────────────────────────────────────────┐
│  ← 返回     我的里程碑                      │
├────────────────────────────────────────────┤
│  漸層 Header：                              │
│  🎉 已解鎖 12 個展演   🏆 連續打卡 3 週      │
│  今日任務：再參觀 1 場即可解鎖「城市探險家」│
├────────────────────────────────────────────┤
│                                              │
│      🌱           🌟           🏛️          🎨 │
│     /  \         /  \         /  \         /  │
│    /    \_______/    \_______/    \_______/   │
│   /                                          \│
│  芽芽初心   →   城市冒險家   →   餉樂漫遊   →   │
│  (已完成)       (進行中)         (鎖定)        │
│                                              │
│  每個 milestone 皆為圓形節點：              │
│  - 中心放置展演代表 icon                    │
│  - 下方顯示日期 + 場館                      │
│  - 已解鎖顯示漸層亮面與動態光圈            │
│  - 未解鎖顯示灰階，搭配小鎖圖示            │
│                                              │
│  路徑使用柔和漸層曲線，背景散落彩色幾何    │
│                                              │
│  下方卡片：                                  │
│  ┌──────────────────────────────────────┐   │
│  │ 當前節點詳情：「城市冒險家」             │
│  │ - 展演名單：故宮×3 / MOCA×1             │
│  │ - 智慧摘要、暖色插圖                     │
│  │ - CTA：回顧票券、分享紀錄                │
│  └──────────────────────────────────────┘   │
└────────────────────────────────────────────┘
```

#### 功能需求
- **Gamified Roadmap**:
  - 曲線里程碑路徑，節點由大到小依序排列
  - 節點具備狀態：`locked`、`inProgress`、`completed`
  - 使用柔和漸層、圓角、發光效果與可愛插畫 icon
  - 進度條顯示距離下一個節點的剩餘場次
- **Milestone 卡片**:
  - 露出代表插畫、稱號、達成條件、已累計場次
  - 可展開查看該里程碑涵蓋的展演紀錄（上下滑動）
  - 支援收藏、分享、重溫展演資訊
- **互動**:
  - 點擊節點觸發彈性動畫（scale、bounce）
  - 未解鎖節點顯示「提示泡泡」說明條件
  - 當累積記錄更新時，觸發解鎖動畫與彩色紙碎效果
- **統計數據**:
  - 總觀展數量、連續打卡週數、最愛場館/類型徽章
  - 提供下一個里程碑倒數，鼓勵回訪
- **記錄方式**:
  - 展演詳情頁「加入里程碑」按鈕；成功後立即更新路徑
  - 支援手動補登觀展日期
- **視覺風格**:
  - Pastel 漸層背景（如 #A6E1FF → #F8D9FF）
  - 可愛向量 icon、簡化圖形
  - 動畫參考 Duolingo：彈跳、滑動、柔和陰影
- **數據存儲**:
  - MVP: LocalStorage
  - 未來: 後端 API + Database（保存里程碑狀態、徽章）

#### 數據來源
- API: `/api/memory/list` (未來)
- LocalStorage: `art-pass-memory` (MVP)

---

## 組件設計

### 共用組件 (`/components`)

#### 1. `EventCard` - 展演卡片

```typescript
interface EventCardProps {
  event: Event;
  layout: 'horizontal' | 'vertical' | 'compact';
  showFavorite?: boolean;
  onFavoriteClick?: (eventId: string) => void;
}
```

**變體**:
- `horizontal`: 探索頁使用，左圖右文
- `vertical`: 首頁使用，上圖下文
- `compact`: 附近頁底部卡片使用

#### 2. `FilterSheet` - 篩選彈窗

```typescript
interface FilterSheetProps {
  isOpen: boolean;
  onClose: () => void;
  onApply: (filters: FilterOptions) => void;
  initialFilters?: FilterOptions;
}

interface FilterOptions {
  region?: string;
  category?: string;
  priceRange?: [number, number];
  timeRange?: 'today' | 'week' | 'month' | 'all';
  venue?: string;
}
```

#### 3. `MapView` - 地圖組件

```typescript
interface MapViewProps {
  events: Event[];
  center: { lat: number; lng: number };
  onMarkerClick?: (eventId: string) => void;
  focusedVenue?: string;
}
```

#### 4. `MilestoneNode` - 里程碑節點

```typescript
interface MilestoneNodeProps {
  milestone: Milestone;
  status: 'locked' | 'inProgress' | 'completed';
  progressPercent: number; // 0 - 100
  onSelect?: (milestoneId: string) => void;
  onUnlockAnimationEnd?: () => void;
}
```

#### 5. `MilestoneDetailSheet` - 里程碑詳情面板

```typescript
interface MilestoneDetailSheetProps {
  milestone: Milestone;
  isOpen: boolean;
  onClose: () => void;
}
```

#### 6. `ScrollableCards` - 橫向滾動卡片容器

```typescript
interface ScrollableCardsProps {
  children: React.ReactNode;
  snapAlign?: 'start' | 'center' | 'end';
}
```

---

## API 設計

### 1. `/api/events/featured` (GET)

**功能**: 獲取熱門展演

**Response**:
```json
{
  "success": true,
  "data": [
    {
      "event_id": "b12c795b-...",
      "title": "TCO音樂會",
      "image_url": "https://...",
      "category": "音樂現場",
      "start_datetime_iso": "2025-11-09T14:00:00+08:00",
      "venue_preview": "臺北市中山堂",
      "ticket_price": "300;500",
      "is_favorited": false
    }
  ]
}
```

**實作方式**: 
- MVP: 讀取 JSON 檔案，隨機或按 ID 排序取前 10 筆
- 未來: 加入瀏覽數、評分等指標

---

### 2. `/api/events/upcoming` (GET)

**功能**: 獲取近期展演

**Query Parameters**:
- `limit`: 數量限制 (預設 10)

**Response**: 同上，但按 `start_timestamp` 排序

---

### 3. `/api/events/list` (GET)

**功能**: 獲取展演列表（含篩選）

**Query Parameters**:
```typescript
{
  region?: string;        // 地區
  category?: string;      // 分類
  minPrice?: number;      // 最低票價
  maxPrice?: number;      // 最高票價
  timeRange?: string;     // today|week|month|all
  venue?: string;         // 場館
  page?: number;          // 分頁（可選）
  limit?: number;         // 每頁數量（預設 20）
}
```

**Response**: 
```json
{
  "success": true,
  "data": [...],
  "pagination": {
    "total": 150,
    "page": 1,
    "limit": 20,
    "hasNext": true
  }
}
```

---

### 4. `/api/events/nearby` (GET)

**功能**: 獲取附近展演

**Query Parameters**:
```typescript
{
  lat: number;          // 緯度
  lng: number;          // 經度
  radius?: number;      // 半徑（公里，預設 10）
}
```

**Response**:
```json
{
  "success": true,
  "data": {
    "venues": [
      {
        "venue_name": "國立故宮博物院",
        "latitude": 25.1024,
        "longitude": 121.5485,
        "distance": 2.3,
        "events": [
          { ... }
        ]
      }
    ]
  }
}
```

**實作**: 使用 Haversine 公式計算距離

---

### 5. `/api/memory/list` (GET)

**功能**: 獲取觀展歷程

**Response**:
```json
{
  "success": true,
  "data": {
    "stats": {
      "totalVisits": 12,
      "currentStreak": 3
    },
    "memory": [
      {
        "visitDate": "2025-11-09",
        "event": { ... }
      }
    ]
  }
}
```

**MVP 實作**: 從 LocalStorage 讀取

---

### 6. `/api/memory/add` (POST)

**功能**: 新增觀展記錄

**Request Body**:
```json
{
  "eventId": "b12c795b-...",
  "visitDate": "2025-11-09"
}
```

**MVP 實作**: 寫入 LocalStorage

---

## 數據流

### 1. 資料格式 (Event)

```typescript
interface Event {
  // 基本資訊
  event_id: string;
  title: string;
  category: string;
  
  // 時間
  date_time: string;
  start_datetime_iso: string;
  end_datetime_iso: string;
  start_timestamp: number;
  end_timestamp: number;
  
  // 地點
  venue_preview: string;
  latitude: number;
  longitude: number;
  google_maps_url: string;
  
  // 票務
  ticket_type: string;
  ticket_price: string;
  ticket_url: string;
  
  // 媒體
  image_url: string;
  local_image_path: string;
  
  // 詳情
  event_description: string;
  organizer: string;
  contact_person: string;
  contact_phone: string;
  event_url: string;
  detail_page_url: string;
}

interface Milestone {
  id: string;
  title: string;              // 例如「城市冒險家」
  description: string;        // 簡短說明或祝賀詞
  icon: string;               // 對應插圖資源
  requiredVisits: number;     // 解鎖所需展演數
  rewardBadge: string;        // 獎勵徽章 ID
  unlockedAt?: string;        // ISO 時間
  events: Event[];            // 該里程碑收錄的展演
}
```

### 2. 前端狀態管理

```typescript
// 使用 Context API (簡單場景)
interface AppState {
  favorites: string[];           // 收藏的展演 ID
  memory: TimelineEntry[];     // 觀展記錄
  filters: FilterOptions;        // 當前篩選條件
  userLocation: {                // 用戶位置
    lat: number;
    lng: number;
  } | null;
}
```

### 3. LocalStorage Keys

```typescript
const STORAGE_KEYS = {
  FAVORITES: 'art-pass-favorites',      // string[]
  TIMELINE: 'art-pass-memory',        // TimelineEntry[]
  LAST_LOCATION: 'art-pass-location',   // { lat, lng }
};
```

---

## 目錄結構

```
art-pass/
├── app/
│   ├── layout.tsx                 # Root Layout
│   ├── page.tsx                   # 首頁
│   ├── globals.css                # 全域樣式
│   │
│   ├── explore/
│   │   └── page.tsx              # 找展演頁
│   │
│   ├── nearby/
│   │   └── page.tsx              # 找身邊頁
│   │
│   ├── memory/
│   │   └── page.tsx              # 找記憶頁
│   │
│   └── api/
│       ├── events/
│       │   ├── featured/
│       │   │   └── route.ts      # 熱門展演 API
│       │   ├── upcoming/
│       │   │   └── route.ts      # 近期展演 API
│       │   ├── list/
│       │   │   └── route.ts      # 展演列表 API
│       │   └── nearby/
│       │       └── route.ts      # 附近展演 API
│       │
│       └── memory/
│           ├── list/
│           │   └── route.ts      # 觀展歷程 API
│           └── add/
│               └── route.ts      # 新增記錄 API
│
├── components/
│   ├── EventCard.tsx             # 展演卡片
│   ├── FilterSheet.tsx           # 篩選彈窗
│   ├── MapView.tsx               # 地圖組件
│   ├── TimelineItem.tsx          # 時間軸項目
│   ├── ScrollableCards.tsx       # 橫向滾動容器
│   └── ui/                       # 基礎 UI 組件
│       ├── Button.tsx
│       ├── Input.tsx
│       ├── Select.tsx
│       └── Slider.tsx
│
├── lib/
│   ├── types.ts                  # TypeScript 型別定義
│   ├── constants.ts              # 常數定義
│   ├── utils.ts                  # 工具函數
│   ├── storage.ts                # LocalStorage 封裝
│   └── geo.ts                    # 地理計算（距離等）
│
├── hooks/
│   ├── useFavorites.ts           # 收藏功能
│   ├── useTimeline.ts            # 觀展記錄
│   ├── useGeolocation.ts         # 地理位置
│   └── useEvents.ts              # 展演資料獲取
│
├── public/
│   ├── data/
│   │   └── events.json           # 展演資料（開發用）
│   └── images/                   # 靜態圖片
│
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.ts
```

---

## 開發階段規劃

### Phase 1: 基礎設施 (1-2 天)

- [ ] 建立型別定義 (`lib/types.ts`)
- [ ] 設定 API 路由基礎結構
- [ ] 建立共用組件骨架
- [ ] LocalStorage 封裝
- [ ] 基礎 UI 組件 (Button, Input 等)

### Phase 2: 首頁 (1 天)

- [ ] 首頁佈局
- [ ] 熱門展演區塊
- [ ] 近期展演區塊
- [ ] 橫向滾動功能
- [ ] Quick Actions 導航

### Phase 3: 找展演 (1-2 天)

- [ ] 展演列表頁面
- [ ] 垂直滾動卡片
- [ ] 篩選彈窗 UI
- [ ] 篩選邏輯實作
- [ ] 無限滾動 / 分頁 (可選)

### Phase 4: 找身邊 (2-3 天)

- [ ] Google Maps 整合
- [ ] 地圖 Markers 顯示
- [ ] 底部卡片橫向滾動
- [ ] 地圖與卡片聯動
- [ ] 智能 Focus 切換場館
- [ ] 場館資訊顯示

### Phase 5: 找記憶 (1-2 天)

- [ ] 建立里程碑資料模型（Milestone）
- [ ] 實作曲線路徑 + 節點動畫
- [ ] 完成 MilestoneNode / MilestoneDetailSheet 組件
- [ ] 統計與徽章區塊（柔和漸層樣式）
- [ ] 里程碑解鎖邏輯（計算剩餘展演數）
- [ ] LocalStorage 持久化與即時更新
- [ ] 解鎖動效與祝賀提醒

### Phase 6: 收藏功能 (0.5 天)

- [ ] 收藏按鈕 UI
- [ ] 收藏狀態管理
- [ ] LocalStorage 存儲

### Phase 7: 優化與調整 (1-2 天)

- [ ] 響應式設計調整
- [ ] 動畫與過渡效果
- [ ] Loading 狀態
- [ ] Error 處理
- [ ] 效能優化
- [ ] Flutter WebView 測試

---

## 技術細節

### 1. 橫向滾動實作

```css
.scroll-container {
  display: flex;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none; /* Firefox */
}

.scroll-container::-webkit-scrollbar {
  display: none; /* Chrome, Safari */
}

.scroll-item {
  scroll-snap-align: start;
  flex-shrink: 0;
}
```

### 2. 地圖與卡片聯動邏輯

```typescript
// 監聽卡片滾動
const handleCardScroll = (index: number) => {
  const currentVenue = venues[currentVenueIndex];
  
  // 如果滾動到最後一張且繼續拉
  if (index === currentVenue.events.length - 1) {
    // 顯示 NEXT 提示
    setShowNextHint(true);
  }
  
  // 如果放開且已經超出，切換到下一個場館
  if (index > currentVenue.events.length - 1) {
    const nextVenueIndex = currentVenueIndex + 1;
    if (nextVenueIndex < venues.length) {
      setCurrentVenueIndex(nextVenueIndex);
      // 地圖 pan 到新場館
      map.panTo(venues[nextVenueIndex].coordinates);
    }
  }
};
```

### 3. 距離計算 (Haversine)

```typescript
function calculateDistance(
  lat1: number, 
  lng1: number, 
  lat2: number, 
  lng2: number
): number {
  const R = 6371; // 地球半徑（公里）
  const dLat = toRad(lat2 - lat1);
  const dLng = toRad(lng2 - lng1);
  
  const a = 
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) *
    Math.sin(dLng / 2) * Math.sin(dLng / 2);
  
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
```

### 4. Google Maps 設定

```typescript
// next.config.ts
module.exports = {
  env: {
    NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: process.env.GOOGLE_MAPS_API_KEY,
  },
};

// 使用 @googlemaps/js-api-loader
import { Loader } from '@googlemaps/js-api-loader';

const loader = new Loader({
  apiKey: process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!,
  version: 'weekly',
});
```

---

## MVP 範圍限制

### ✅ 包含功能
- 4 個核心頁面
- 基本篩選
- 收藏功能（LocalStorage）
- 觀展記錄（LocalStorage）
- Google Maps 整合
- 響應式設計

### ❌ 不包含功能（未來版本）
- 用戶登入 / 註冊
- 社交分享
- 評論 / 評分
- 通知推播
- 後端數據庫
- 搜尋功能（可用篩選替代）
- 多語言支援
- 深色模式

---

## 環境變數設定

創建 `.env.local`:

```bash
# Google Maps
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_api_key_here

# API Base URL (optional)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Data Source
DATA_SOURCE_PATH=public/data/events.json
```

---

## 測試與部署

### 開發環境
```bash
cd art-pass
npm install
npm run dev
```

### 建置與部署
```bash
npm run build
npm run start
```

### Flutter WebView 整合

在 TownPass 中載入：
```dart
WebView(
  initialUrl: 'http://localhost:3000', // 開發環境
  // initialUrl: 'https://your-domain.com', // 正式環境
  javascriptMode: JavascriptMode.unrestricted,
)
```

---

## 下一步

1. **閱讀此文件**，確認架構與需求
2. **開始開發** Phase 1 (基礎設施)
3. **逐步實作**各頁面功能
4. **在 Flutter WebView 中測試**
5. **收集反饋並迭代**

---

## 聯絡資訊

- 專案路徑: `/Users/charless/Development/2025-Hackthon-4dimo`
- Next.js App: `art-pass/`
- Flutter App: `TownPass/`

---

**最後更新**: 2025/11/08
**版本**: MVP v1.0
**狀態**: 📝 規劃完成，待開發

